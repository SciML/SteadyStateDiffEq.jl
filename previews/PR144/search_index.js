var documenterSearchIndex = {"docs":
[{"category":"section","location":"#SteadyStateDiffEq.jl","page":"Home","text":"SteadyStateDiffEq.jl provides algorithms for solving steady-state problems in the SciML ecosystem.","title":"SteadyStateDiffEq.jl"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(\"SteadyStateDiffEq\")","title":"Installation"},{"category":"section","location":"#Usage","page":"Home","text":"Use SSRootfind to solve the steady-state residual equation with a nonlinear solver:\n\nusing SteadyStateDiffEq, NonlinearSolve\n\nsol = solve(prob, SSRootfind(NewtonRaphson()))\n\nUse DynamicSS to integrate the system until its derivative is close to zero:\n\nusing SteadyStateDiffEq, OrdinaryDiffEq\n\nsol = solve(prob, DynamicSS(Tsit5()))\n\nUse SICNM (the semi-implicit continuous Newton method) to solve the steady-state residual equation by integrating the continuous Newton flow, written as a differential-algebraic equation, until the residual is close to zero. This is much more robust than Newton's method on ill-conditioned problems such as power flow equations:\n\nusing SteadyStateDiffEq, OrdinaryDiffEqRosenbrock\n\nsol = solve(prob, SICNM(Rodas3d()))","title":"Usage"},{"category":"section","location":"#API","page":"Home","text":"","title":"API"},{"category":"type","location":"#SteadyStateDiffEq.SSRootfind","page":"Home","text":"SSRootfind(alg = nothing)\n\nSolve a steady-state problem by converting it to a NonlinearProblem and calling a nonlinear solver.\n\nArguments\n\nalg: the nonlinear solver algorithm passed to solve. When alg === nothing, the default nonlinear solver is selected by the downstream solver package.\n\nFields\n\nalg: nonlinear solver algorithm, or nothing to request downstream default selection.\n\nExample\n\nusing SteadyStateDiffEq, NonlinearSolve\n\nsol = solve(prob, SSRootfind(NewtonRaphson()))\n\nnote: Note\nThe default alg of nothing works only if NonlinearSolve.jl is installed and loaded.\n\n\n\n\n\n","title":"SteadyStateDiffEq.SSRootfind"},{"category":"type","location":"#SteadyStateDiffEq.DynamicSS","page":"Home","text":"DynamicSS(alg = nothing; tspan = Inf)\n\nSolve a steady-state problem by evolving the corresponding ODE until the derivative is close to zero.\n\nDynamicSS internally adds a TerminateSteadyState callback. The abstol and reltol keywords passed to solve control the steady-state termination condition. Use odesolve_kwargs to pass separate keyword arguments to the ODE solve.\n\nArguments\n\nalg: the ODE solver algorithm passed to solve. When alg === nothing, the default ODE solver is selected by the downstream solver package.\n\nKeyword Arguments\n\ntspan: the time span used for the ODE solve. If tspan is a number, it is equivalent to (zero(tspan), tspan).\n\nFields\n\nalg: ODE solver algorithm, or nothing to request downstream default selection.\ntspan: time span passed to the internal ODE solve.\n\nExample\n\nusing SteadyStateDiffEq, OrdinaryDiffEq\nsol = solve(prob, DynamicSS(Tsit5()))\n\nusing Sundials\nsol = solve(prob, DynamicSS(CVODE_BDF()); dt = 1.0)\n\nnote: Note\nThe default alg of nothing works only if DifferentialEquations.jl is installed and loaded.\n\nnote: Note\nIf you use CVODE_BDF you may need to give a starting dt via dt = .....\n\n\n\n\n\n","title":"SteadyStateDiffEq.DynamicSS"},{"category":"type","location":"#SteadyStateDiffEq.SICNM","page":"Home","text":"SICNM(alg; tspan = Inf)\n\nThe Semi-Implicit Continuous Newton Method for solving a steady-state (or nonlinear) problem 0 = g(y). The problem is converted into the differential-algebraic system\n\nbeginaligned\ndoty = z\n0 = J(y) z + g(y)\nendaligned\n\nwith the consistent initialization z(0) = -J(y_0)^-1 g(y_0), where J is the Jacobian of g. The exact solution of this DAE is the continuous Newton flow, along which g(y(t)) = g(y_0) e^-t, so integrating to steady state solves the nonlinear system. Because the DAE is integrated with an implicitly stable method with step size control, this is significantly more robust than Newton's method (which corresponds to integrating the same flow with explicit Euler steps), at the cost of more work per step. It is particularly effective on ill-conditioned problems such as power flow equations where Newton's method diverges.\n\nSICNM internally adds a callback that terminates the integration when the nonlinear residual g(y) satisfies the termination condition. The abstol and reltol keywords passed to solve control this termination condition — i.e. how small g(y) must be for the returned point to count as a solution.\n\nThe accuracy of the transient DAE integration is a separate knob. Because only the steady state matters (the trajectory that reaches it does not), the ODE solve defaults to a loose tolerance so the stiffly accurate solver can take large damping steps toward equilibrium, exactly as the reference SICNM implementation does. Tying the integration tolerance to the (tight) residual tolerance would force an accurate transient and cost roughly an order of magnitude more work for the same answer. Pass odesolve_kwargs = (abstol = ..., reltol = ...) to override the integration tolerance, along with any other keyword arguments for the ODE solve.\n\nThe Jacobian-vector products J(y) z required by the DAE are computed via ForwardDiff dual numbers together with the residual evaluation, so no full Jacobian of g is ever materialized inside the right-hand side. A single dense Jacobian is formed only for the consistent initialization z(0) = -J(y₀)⁻¹ g(y₀), whose linear solve is handled by LinearSolve.jl and is configurable through the linsolve keyword.\n\nArguments\n\nalg: the ODE solver algorithm used to integrate the DAE. It must support mass matrices and should be stiffly accurate and L-stable. Rodas3d from OrdinaryDiffEqRosenbrock.jl was constructed specifically for this method and is the recommended choice; other Rosenbrock methods such as Rodas4 or Rodas5P also work well. The linear solver used for the implicit stages of the DAE integration is configured on this ODE algorithm itself (e.g. Rodas3d(; linsolve = KrylovJL_GMRES())).\n\nKeywords\n\ntspan: the time span used for the ODE solve. If tspan is a number, it is equivalent to (zero(tspan), tspan). Since the residual decays like e^-t along the exact flow, the default Inf combined with the termination callback is normally appropriate.\nlinsolve: the LinearSolve.jl algorithm used for the consistent initialization solve z(0) = -J(y₀)⁻¹ g(y₀). Defaults to nothing, which lets LinearSolve select an appropriate factorization for the Jacobian. Pass e.g. linsolve = KrylovJL_GMRES() for large or sparse systems.\n\nExample\n\nusing SteadyStateDiffEq, OrdinaryDiffEqRosenbrock\n\nsol = solve(prob, SICNM(Rodas3d()))\n\nReferences\n\nYu, R., Gu, W., Xu, Y., Lu, S. (2024). Semi-implicit Continuous Newton Method for Power Flow Analysis. arXiv:2312.02809. https://arxiv.org/abs/2312.02809\n\n\n\n\n\n","title":"SteadyStateDiffEq.SICNM"}]
}
